/*
#include "Base.h"

template <typename T>
std::vector<T> Sort(std::vector<T> unsorted_vector) {
	std::vector<T> sorted_vector = { unsorted_vector[0] };
	for (unsorted_item : unsorted_vector) {
		for (sorted_item in sorted_vector) {
			if (sorted_)
		}
	}
}
*/